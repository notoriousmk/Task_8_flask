"'./globals.css"

const metadata = {
  e: "nextjs Tailwind project",
  ription: "This is internship project",
}
export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body >{children}</body>
    </html>
  );
}
